<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand">FBI System</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">      
      <ul class="navbar-nav ml-auto">
        {{#if user}}
            <li class="nav-item">
                <a class="nav-link" id="dashboard" href="/dashboard?token={{token}}">Visita tu Dashboard</a>
            </li>            
        {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/">Volver</a>
          </li>        
        {{/if}}
      </ul>
    </div>
  </div>
</nav>


<script>
  sessionStorage.removeItem('token');
  sessionStorage.setItem('token', '{{token}}');
  document.getElementById('dashboard').addEventListener('click', async(e)=>{
    {{!-- e.preventDefault();     --}}
    await fetch(`/dashboard?token=${token}`)
  });
</script>